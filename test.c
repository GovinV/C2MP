#pragma MPC 	precision(       	215     ) rounding(       				dez)
{
	azerty = ++++a - ++e;
	azerty = a+b+c+a*d*f+e+f+g;
	azerty = a == 3 && (a==1 || a==3) && a==3 || a==1&&a==3&&a==3&&a==3;

    a = 0.;
    while(a < 10 || 1)
    {
        a=a+1;
    }
}
